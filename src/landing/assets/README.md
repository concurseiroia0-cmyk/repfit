# Assets da landing

Coloque aqui as imagens da página de vendas (capa da mini VSL, fotos de
depoimentos, banners etc.) e referencie com `import` no componente.

Exemplo:

```ts
import vslCover from '../assets/vsl-cover.jpg';
```
